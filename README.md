TypeScript Microservice for JPEG2TXT ocr

Build:

`npm run-script build`

Run server:

`npm run-script server`


See ske/ts-jobserver-simple for details.

Dependencies:
- unzip
- tesseract + tesseract data for a given language
